import React from 'react';
import AppNavigator from './App_Navigator'; // Ensure the path matches where your AppNavigator file is located

const App = () => {
  return <AppNavigator />;
};

export default App;
